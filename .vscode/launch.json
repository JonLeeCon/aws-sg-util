{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug",
      "type": "lldb",
      "request": "launch",
      "program": "${workspaceRoot}/target/debug/aws-sg-util",
      "args": [
        // "-h"
        // "-l"
        // "-l", "--sg", "XXX"
        // "-a", "https", "--sg", "XXX"
        // "-a", "https"
        "-r", "https", "--sg", "XXX"
      ],
      "cwd": "${workspaceRoot}",
      "sourceLanguages": ["rust"]
    }
  ]
}